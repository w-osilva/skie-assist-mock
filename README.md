# README

This is a simple Rails application to simulate the integration with Skie Assist API. 

## Usage

- Clone the repository to your local machine.
- Fetch the dependencies. `bundle install`
- Run the server. `bundle exec rails s -p 8081 -b 0.0.0.0`
